import React from 'react';
import { Video } from '../types';
impprt {NextPage} from ''
interface IProps{
    post:Video;
}
function VideoCard({post}:IProps) {
    return (
        <div>
            
        </div>
    );
}

export default VideoCard;