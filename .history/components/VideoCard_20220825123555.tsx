import React from 'react';
import { Video } from '../types';
interface IProps{
    post:Video
}
function VideoCard({post}) {
    return (
        <div>
            
        </div>
    );
}

export default VideoCard;